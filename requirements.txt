# --- Core DL ---
torch>=2.3.0
torchvision>=0.18.0

# --- Face models / CV ---
insightface>=0.7.3
ultralytics>=8.2.0        # YOLO-Face detector (FACECORE_DETECTOR=yolo)
onnx>=1.16.0
onnxruntime-gpu>=1.18.0   # falls back to CPU EP automatically if no CUDA
opencv-python-headless>=4.10.0
numpy

# --- Vector search ---
faiss-cpu>=1.8.0          # swap for faiss-gpu in CUDA images

# --- API ---
fastapi>=0.115.0
uvicorn>=0.49.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- MLOps / training ---
tensorboard>=2.17.0
tqdm>=4.66.0
pyyaml>=6.0.1
python-dotenv>=1.0.1

# --- Quality / tests ---
pytest>=8.2.0
ruff>=0.5.0
